// template literals:

// template literals are a way to create strings that can contain placeholders for variables



const name = 'John';
const age = 20;
const city = 'New York';

const message = `My name is ${name} and I am ${age} years old. I live in ${city}.`;

console.log(message);